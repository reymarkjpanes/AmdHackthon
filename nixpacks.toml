[phases.setup]
aptPkgs = ["tesseract-ocr", "libgl1", "libglib2.0-0"]

[phases.install]
cmds = ["pip3 install -r backend/requirements.txt"]

[start]
cmd = "cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
